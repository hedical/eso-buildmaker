import React from 'react'
import ReactTooltip from "react-tooltip";

const Tooltip = (props) => {
    return (
        <>
            <h1>{props.piece}</h1>
        </>
    )
}

export default Tooltip
