import React from 'react'

const AuthOptions = () => {
    return (
        <div>
            <button>Register</button>
            <button>Log in</button>
        </div>
    )
}

export default AuthOptions
